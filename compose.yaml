services:
  database:
    image: postgres:18-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    env_file: "./database/container/.env"
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./database/initdb.d:/docker-entrypoint-initdb.d

volumes:
  postgres_data: