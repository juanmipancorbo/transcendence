services:
  database:
    image: postgres:18-alpine
    restart: always
    ports:
      - "5432:5432"
    env_file: "./database/container/.env"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/inidb.d:/docker-entrypoint-initdb.d

volumes:
  postgres_data: